{
  "$schema": "http://json-schema.org/schema",
  "id": "ngx-pointdeb-material",
  "title": "Material initializer",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "the project to initialize",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
